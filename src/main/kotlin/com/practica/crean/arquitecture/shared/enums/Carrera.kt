package com.example.demo.shared.enums

/*
* Las clases enum en Kotlin representan un conjunto fijo de constantes,
* útiles para definir tipos con un número limitado de valores posibles.
* */
enum class Carrera {

    /*
    * Las comillas invertidas (``) en Kotlin se usan para permitir nombres
    * de variables, funciones, y propiedades que coinciden con palabras
    * reservadas o contienen caracteres no alfanuméricos, facilitando la
    * interoperabilidad con APIs de Java y otros casos especiales.
    * */
    `Ingeniería de Software`,
    `Administración`,
    `Contaduría`,
    `Gestión`

}